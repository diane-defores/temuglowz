---
import DiscoveryIcon from './DiscoveryIcon.astro'

interface Guide {
  slug: string
  title: string
  homepageTitle?: string
  description: string
  category: string
  readingHint: string
}

interface Props {
  guides: Guide[]
}

const { guides } = Astro.props
---

<section id="selections" class="scroll-mt-28 py-16 sm:py-24" aria-labelledby="selections-title">
  <div class="mx-auto max-w-6xl px-4 sm:px-6">
    <div class="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between">
      <div class="max-w-3xl">
        <p class="text-sm font-medium uppercase tracking-wider text-muted-foreground">Nos trouvailles du moment</p>
        <h2 id="selections-title" class="mt-3 font-instrument-sans text-3xl font-bold text-foreground sm:text-5xl">Commencez par celles-ci.</h2>
      </div>
      <a href="/guides" class="inline-flex items-center gap-2 text-sm font-medium text-foreground underline decoration-border underline-offset-4 hover:decoration-foreground focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-foreground">Voir toutes les trouvailles <span aria-hidden="true">→</span></a>
    </div>

    {guides.length > 0 ? (
      <div class="mt-10 grid gap-5 md:grid-cols-2">
        {guides.map((guide, index) => (
          <article class="group relative flex flex-col justify-between overflow-hidden rounded-3xl border border-border bg-card p-7 sm:p-9 transition-all duration-300 hover:border-primary/30 hover:shadow-lg">
          <div>
            <span class="rounded-full bg-muted px-3 py-1 text-xs font-medium uppercase tracking-wider text-muted-foreground">{guide.category}</span>
            <h3 class="mt-4 text-2xl font-semibold text-foreground sm:text-3xl">{guide.homepageTitle || guide.title}</h3>
            <p class="mt-3 leading-relaxed text-muted-foreground">{guide.readingHint || guide.description}</p>
          </div>
          <div class="mt-6 flex items-center justify-between gap-4">
            <a href={guide.slug} class="inline-flex h-11 items-center justify-center gap-2 rounded-full bg-primary px-6 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-foreground">
              Je veux voir <span aria-hidden="true">→</span>
            </a>
            <div class="shrink-0 text-primary/80 group-hover:scale-110 transition-transform duration-300">
              <DiscoveryIcon name={index === 0 ? 'kitchen' : 'wind'} />
            </div>
          </div>
        </article>
        ))}
      </div>
    ) : (
      <p class="mt-10 rounded-3xl border border-dashed border-border bg-card p-7 text-muted-foreground">De nouvelles trouvailles arrivent bientôt. Revenez jeter un œil&nbsp;!</p>
    )}
  </div>
</section>
